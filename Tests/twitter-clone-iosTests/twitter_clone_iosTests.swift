import XCTest
@testable import twitter_clone_ios

final class twitter_clone_iosTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(twitter_clone_ios().text, "Hello, World!")
    }
}
